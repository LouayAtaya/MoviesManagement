import { UserForAuthentication } from './user-for-authentication';

describe('UserForAuthentication', () => {
  it('should create an instance', () => {
    expect(new UserForAuthentication()).toBeTruthy();
  });
});
